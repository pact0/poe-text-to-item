{
  "name": "poe-text-to-item",
  "version": "1.0.2",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist/**"
  ],
  "author": "pact0 <kuba.kowalski21331@gmail.com>",
  "description": "Parser for Path of Exile item text.",
  "license": "MIT",
  "scripts": {
    "dev": "npx ts-node-dev --respawn --prefer-ts true --no-notify -r tsconfig-paths/register --watch src --transpile-only src/index.ts",
    "build": "tsc && tscpaths -p tsconfig.json -s ./src -o ./dist"
  },
  "devDependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.14.1",
    "tscpaths": "^0.0.9",
    "ttypescript": "^1.5.13",
    "typescript": "^4.6.3",
    "typescript-transform-paths": "^3.3.1"
  }
}
